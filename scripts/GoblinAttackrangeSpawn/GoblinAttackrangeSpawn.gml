function GoblinAttackrangeSpawn() { 
	var _spd = Enemyspeed;

	 _spd=0; 
if (image_index<=0.2){ 
instance_create_layer(x+random_range(-32,+32),y+random_range(-32,+32),"Instances",Suicidebomberfirepool)
spawn++



	}

	if (floor (image_index) ==8)
	{stateTarget= EnemySTATE.CHASE;
		sateWaitDuration = 60;
		state = EnemySTATE.WAIT;
spawn=0
	}
	}

	



