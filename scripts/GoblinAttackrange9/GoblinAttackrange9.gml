function GoblinAttackrange9() { 
	var _spd = Enemyspeed;
 
	if (image_index<2) _spd=0;
	if (floor(image_index)== 4) 
	if !instance_exists(beamtarget)

	with (instance_create_layer(OGoblin.x-32,OGoblin.y-32,"Instances",beamtarget))
	{

		targetX = OGoblin.x;  
		targetY = OGoblin.y-16;
		direction = point_direction(x,y,targetX,targetY);
		distanceTotal = 10000000;
		distanceRemaining = distanceTotal;
	peakHeight = min(max(0,distanceTotal - 64)*0.2,150);
			spd = 1;
	}
	
	

	if (floor (image_index) ==5)
	{stateTarget= EnemySTATE.CHASE;
		sateWaitDuration = 5;
		state = EnemySTATE.WAIT;

	}
	}

	



